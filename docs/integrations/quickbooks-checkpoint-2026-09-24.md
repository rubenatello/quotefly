# QuickBooks checkpoint — September 24, 2026 (UTC)

## Saved baseline and staging recovery

Feature commit `f82c6e8f02f4bc29c7dbdeaebf89ff388571c9da` contains the authenticated internal tax-review assembler and context-bound claim validation. Independent source review approved that exact candidate. Local `verify:ci` passed with 402 unit tests and 561 database tests; GitHub CI run `35938009769` also completed successfully. Main remains unmerged.

The isolated staging migration job `2177175c-881d-4f93-b298-0c650f0cfbfc` applied the context and review-binding migrations, then exited unsuccessfully at its post-migration catalog check. The checker expected 12 context-table constraint rows; PostgreSQL also records the two intentionally deferred constraint triggers there, making 14. This was a checker defect, not a failed SQL migration. Preserve the failed deployment as incident evidence. Do not rerun that one-shot job or perform a down migration.

The independent read-only recovery check at `2026-09-24T00:57:52.515Z` passed: migration history 97 total / 96 completed / one historical rollback / zero unresolved, exact new migration checksums, preserved prior-history digest, 37 forced-RLS tables, 46 tax-operation columns with only 19 runtime-update columns, and the exact 12 ordinary plus two deferred trigger constraints. Both context tables are empty. Existing counts remain 39 customers, six quotes, one job and one internal invoice; provider-operation rows remain empty.

Sentinel and Opera separately approved deploying only the immutable f82 staging API after reviewing this recovery evidence. The later capture endpoint and UI work are outside that deployment approval. OAuth-only mode, reconciliation/CDC disablement and hosted-payment disablement remain mandatory. Production is outside this approval.

That exact API rollout completed as deployment `ab69878b-ba8f-4dac-b3d2-ff20a82bdadc`. All 19 critical deployed source hashes match f82. Runtime checks confirm the sandbox/OAuth-only safeguards, verifier and encryption-key presence, absence of the owner database URL, and the restricted non-superuser/non-bypass runtime role. `/v1/health` and `/v1/ready` returned 200 at `01:07:05.908Z` and `01:07:06.148Z`. A further read-only check at `01:06:13.697Z` passed after restricting the catalog correction to the context tables; the tax-operation boundary query remains unchanged. Frontend, workers and production were not part of this rollout.

## Capture API candidate

The next backend increment implements authenticated manager GET/POST at `/v1/integrations/quickbooks/invoices/:invoiceId/tax-context`. Responses, including errors, are private/no-store. GET supplies display-only invoice details, editable address suggestions and a short-lived purpose-bound form token. POST accepts explicit date, addresses, local line tax intent, expected revision and a UUID command key. It reloads all financial, lifecycle, setup, connection and mapping authority under the publication lock.

The source token uses a separate HKDF/HMAC purpose, strict encoding and lifetime bounds, exact current/previous key IDs and constant-time verification. It contains no addresses, provider IDs or credentials. Live tenant membership and authorization are rechecked inside the transaction. An unchanged command can replay after a lost successful response and token reissue without duplicating context revisions.

The increment makes no provider calls, changes no invoice totals and returns `taxCalculationProven: false` and `publishingAuthorized: false`. Independent Sentinel review approved the frozen six-file increment. Full local `verify:ci` passed at `2026-09-24T01:12:50.713Z`: 406 unit tests and 574 database tests, backend/frontend builds, lint, schema validation, route/security checks, parser evaluations and dependency audits. Final Opera review remains pending at this checkpoint.

## Separate UI work and remaining release evidence

The tax-details form remains in the isolated `feature/quickbooks-tax-capture-20260923` worktree. It is not part of the saved f82 release or the six-file backend increment. Review found and is addressing mobile action-bar overlap, unsaved-navigation coverage, browser Back listener ordering, job-refresh preservation and keyboard focus across overlay handoffs. Do not describe that UI as released until its own tests and final review pass.

Real positive-tax Estimate behavior and linked-Invoice parity, signed webhook delivery/recovery, payment/reconciliation scenarios, successful alert delivery and the complete production gate remain open. Intuit developer sign-in and replacement of the invalid Resend credential still require owner action. Public claims remain limited to functionality actually available; no tax, synchronization or production-readiness claim is authorized by this checkpoint.

## Capture increment saved; UI candidate under verification

Opera subsequently approved the exact seven-file capture backend/checkpoint manifest without findings for feature-branch BCP. It was committed and pushed as `cef54275bf22202a5905660a9a5c3d4348de130c`. GitHub CI run 126 (`35942616606`) completed successfully, verified at `01:48:11Z`. This commit has not been deployed; staging remains on f82.

The next source candidate brings the private tax form into InvoicePanel on both quote and job pages. It collects date, separate structured US origin/destination and explicit line tax intent. Draft addresses and source tokens stay in memory. Reload preserves edited decisions; a lost successful response reuses the same semantic command identity. Capture still returns false tax-calculation and publishing authority and does not call QuickBooks.

Independent reviews prompted corrections for fixed mobile controls, overlay focus, browser Back listener ordering, same-record refresh failures, stacked retention confirmations, and navigation during in-flight tax saves or checkout responses. The shared workspace guard joins sidebar, mobile navigation, search, notifications and Kody to each page's existing prompt. Submitted saves block departure until their outcome is known; PDF preview and checkout entry stay unavailable while edits are pending. Archive/delete wait for explicit discard confirmation before mutation. Transient quote/job refreshes preserve the mounted tax form; authorization and missing-record responses still clear inaccessible records.

The broader affected browser run passed 43 cases before the final race fixes. A subsequent run passed 25 of 26 cases and exposed a mobile Kody test/return-target assumption: the contextual action replaces the generic launcher on narrow pages. The corrected 26-case run passed at `01:35:37.532Z`. A further eight-case run against the final source passed at `01:37:57.905Z`, including blocked in-flight saves, desktop/mobile browser Back, workspace handoffs, and visible focus return for Kody workspace, booking and dispatch actions. The final source uses the visible origin control and centralizes mobile navigation handoff. The combined main-worktree `npm run verify` passed at `01:40:45.174Z`, including builds, lint, schema, security/route checks, evaluations and audits. Independent final reviews remain pending here. No UI release or production approval is asserted by this checkpoint.

## Final-review remediation

Opera returned `CHANGES_REQUIRED` for the first 24-file UI candidate. Confirming tax details could overwrite unreviewed mapping or payment edits, and a denied job refresh could leave private tax fields mounted. The replacement candidate makes tax capture and unreviewed mapping/payment changes mutually exclusive, with inline English/Spanish explanations and defensive handler checks. Pending or saving tax details pause customer, item and payment controls. An unreviewed mapping or payment change pauses tax capture without clearing its values.

Jobs now retain the same private draft only for network/5xx refresh failures. A 401, 403 or 404 clears the selected record and unmounts the form. The retained refresh branch shows progress, a localized failure and Retry while preserving the stable job component. Browser regressions cover denial, transient failure/retry, both mapping/payment edit orders, and a held tax save.

Initial verification caught two test assertions using an incorrect reset label or treating a fieldset itself as a disabled control; those assertions were corrected to the visible label and the actual descendant controls. TypeScript also required an explicit selected-job null guard. A later parallel verification run hit the one-second local Redis timeout, while an early browser list load exceeded its ten-second wait. These failed runs remain evidence; final verification is being run sequentially and no failure is waived.

The separate ignored sandbox preflight passed independent security review and 11 mocked cases. It validates the exact staging identity before loading credentials, checks the synthetic fixture and current manager/context/source both before and after provider reads, and builds only an in-memory Estimate payload. It performs no provider accounting writes or tax-ledger claims. OAuth token refresh may use the existing encrypted credential lifecycle. No hosted preflight, positive tax calculation, linked-Invoice parity or production approval is claimed.

Final verification completed successfully: the 37-case browser run passed 36 cases, and its initial job-list timeout passed in the sequential 20-case recovery run. That recovery run passed 19 cases; its remaining test used "Retry" instead of the existing "Try again" label. The corrected final transient-refresh test passed at `02:14:01.829Z`, including held-GET loading feedback, visible error/retry, and preserved private fields after success. Existing mobile mapping pagination, stale lookup fencing, and payment-status refresh also passed. These follow-ups resolve every failed case without changing timeout limits or skipping tests.

The exact final source passed `npm run verify` from `02:14:24.351Z` through `02:17:13.427Z`, including the previously failed Redis replica-rate-limit check. Preserved receipt: `.codex_tmp/tax-ui-opera2-verify-final-20260924-result.json`. Sentinel and Goldface approved the 23-file UI manifest `250a3cc45eaf50f0c29a66ad18a484ffa771c64f2f45684cbb56fddda6beb700` (22 UI/test changes plus an unchanged inherited fixture helper). Final Opera review of the complete 24-file release candidate, including these two documentation changes, remains required before BCP. Production and provider-write enablement remain outside this increment.
