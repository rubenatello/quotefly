# QuickBooks staging continuation — September 23, 2026

Latest saved source checkpoint: `6e25a40493998f2b2e82e84282a2e0864d0c73a6`, deployed to the isolated staging API/web, with exact-source CI run 119 SUCCESS. Monitor c7252ff and accounting worker 98c7575 are RUNNING after the bounded failure tests. Production and provider writes remain blocked on the owner/provider evidence listed near the end of this record. The earlier entries below are chronological checkpoints, not the current deployment state.

Resume candidate: `98c75753d0931433b284ae7f190a2ceea978e113`, branch `feature/quickbooks-staging-acceptance-20260913`, draft PR #9. Fresh fetch confirmed unchanged `origin/main` at `1d987c16ec07804a9404c47648d28e0f5ca3be11` and a clean worktree at resumption. Exact-commit GitHub CI run 115 (`34804967612`) completed successfully. The prior local launch gate and formatting-equivalence proof remain applicable.

Opera independently rereviewed the committed migration and runtime boundary: APPROVED for the bounded isolated staging rollout, with fresh provider-state checks required. This does not approve production or invoice publishing before hosted operational prerequisites.

## Fresh preflight

- Railway authentication works. The owner confirmed `rubenatello@gmail.com` is saved and verified for the account's operational notifications. Actual delivery remains a separate test.
- Existing API deployment remains `8a8c5caf-03a4-4b93-b066-fae80fd74b98` (9545836), SUCCESS.
- Environment `90d918bf-8432-4cf5-89e0-6166cb46468c` is staging, with no source environment. The new monitor has exactly one service instance there and no existing deployment/source.
- Migration and worker service commands match the prior reviewed setup. Migration is a one-shot command with no HTTP health check; the worker uses its dedicated start command.
- Running API presence-only audit confirms sandbox credentials/key/verifier present, migration-owner URL absent, OAuth-only mode true, and reconciliation/CDC/payments false. The old OAuth audit profile still flags the intentionally configured verifier; its overall result is not claimed as passing.
- Read-only database proof confirms forced RLS and actual restricted `quotefly_runtime`, with neither monitor table present before migration.
- The staging browser session expired; the owner was asked to sign into the existing account. No password was retrieved or requested in chat.

## Artifact and rollout tracking

The first archive used Windows Git's CRLF export conversion. Its upload began before the asynchronous archive-verification process had returned; verification then identified byte differences. The deployment (`1d09343a-5707-4fa8-8a06-8a64cf8c568e`) was explicitly cancelled and subsequently reported REMOVED. The read-only database proof still showed both monitor tables absent. Future dependent operations must wait for the archive command's final exit before uploading.

A fresh `git -c core.autocrlf=false archive` was fully verified before upload: all 82 committed source hashes match `f434118d41cad2bbb09945267eee474588a01f91b717eaebf7d9b8776eeaf025`, and no environment files are present. The archive lives outside the workspace at the ignored local temporary path ending `quotefly-staging-98c7575-lf-20260923`.

- Corrected migration deployment: `9092bc82-7999-4573-9212-490fa0f19b19`; completion pending at this checkpoint.
- Matching Vercel staging build: `dpl_FoJDB7wSsZV4kexAAg7yvC7Hn7fJ`, READY, not yet promoted. This uses only the isolated `quotefly-staging` project.
- Worker-only flags prepared with deployment deferred: provider and reconciliation true, OAuth-only false, CDC false. API flags remain unchanged.

No production deployment, accounting write, or real-provider acceptance claim is made by this checkpoint. Continue the sequence in the September 13 handoff, recording actual results below as they become available.

## Migration and API/web completed

- Corrected migration deployment reached SUCCESS. Sanitized logs at 17:21:29–17:21:30 UTC identify the monitor migration and confirm all migrations successfully applied.
- API deployment `5ff4eab8-9de7-4565-9541-1aabc3fd88f2` reached SUCCESS with `npm start` and `/v1/ready`. Both staging health/readiness probes returned HTTP 200.
- Vercel staging deployment `dpl_FoJDB7wSsZV4kexAAg7yvC7Hn7fJ` was promoted successfully within the staging project.
- Hosted read-only proof passed: actual restricted runtime role; all 34 expected tenant tables have enabled and forced RLS; both monitor tables allow SELECT/INSERT/UPDATE and deny DELETE. Both worker heartbeats were absent before startup, as expected.
- Complete active-tenant pre-start scan found one connected sandbox company, zero setup-confirmed companies, zero outstanding/dead webhooks, zero reconciliation-required operations, zero connection/orphan revocations, and zero scheduled/claim-candidate work. Counts do not assert token validity or accounting permission. Local sanitized evidence: `.codex_tmp/qbo-hosted-prestart-20260923.json`.
- Fresh Sentinel resumption check passed: current root/web registry audits have zero advisories, policy exceptions remain empty, and 23 focused worker/monitor boundary tests passed. No reviewed source/config changes were made.
- Reconciliation worker deployment `1db63ef4-29be-4ecc-8b68-8eb5d00a265d` and monitor deployment `9a321c7e-0339-47ea-b83e-a6db8ebbeee1` are building at this checkpoint. Resolved starts are their dedicated commands, with no HTTP health checks. Monitor is enabled with reconciliation/CDC expectation false during initial startup; expectation will change only for the controlled worker monitoring test.

## Worker configuration and monitor diagnosis

- Railway marked the initial worker deployment successful, but the application correctly refused legacy unrelated Stripe/Resend credentials in its restricted environment. A temporary fixed-message diagnostic start identified configuration failure without emitting values. The worker-only unused credentials were cleared, NODE_ENV set to production, and the dedicated worker command restored. A redeploy reused the prior command manifest; a fresh archive upload was required to apply the service command change.
- Worker deployment `6a45cdc0-e715-456e-9e6a-f6a36e82fb2e` runs the reviewed 98c7575 artifact. Presence-only worker profile and actual runtime RLS/grant checks pass. Persisted RUNNING heartbeats began at 17:37:13 UTC and remain fresh. Workflow invocation counts include no-op tenant scans; they are not provider request counts.
- Initial monitor deployment ran successfully with worker expectation false. Deployment `cc26fe77-26a1-4eef-ada3-074d25ee5151` enables the reconciliation expectation; CDC remains false. Fixed presence evidence confirms the verified recipient and staging label.
- The new monitor has intermittent FAILED cycles. Successful samples scan 11 active tenants, queue zero alerts and report zero terminal deliveries. A read-only diagnostic localized a failure to `loadQuickBooksOperationalHealth`: Prisma P2028 after approximately 4.8 seconds. Transaction timeout remediation and hosted verification remain pending. The SSH diagnostic projects only the monitor's expected environment keys because injected SSH configuration is outside the daemon profile.
- No controlled worker stop, OPEN/RECOVER alert delivery, inbox receipt, or quiet-window acceptance has yet been completed. The worker remains running during monitor diagnosis. No invoice publishing or production deployment occurred.

## Tax capability preflight checkpoint

Four source files add an unexposed, read-only company/preferences capability helper and its unit gate. It validates the company binding before reading preferences and returns only fixed prerequisite booleans/reason codes. It always reports automated tax calculation as unproven. No route, schema, workflow flag, or accounting write is enabled.

Independent backend and Opera reviews approved this bounded checkpoint for BCP to the staging branch. Full local `npm run verify` passed, including 289 unit tests, build, lint, schema, security checks, evaluations, and dependency audits. An earlier attempt failed because Docker/Redis was stopped; after starting the dedicated test services the complete gate passed. The old Redis port was unavailable, so a separate isolated Redis container uses local port 16379.

Actual sandbox company-country/address and preference representation still need a sanitized read-only fixture before any caller uses the helper. Exact-commit remote CI remains required after push. Hosted services remain on 98c7575; this review is not production or tax-calculation approval.

Checkpoint committed and pushed as `0daffc672966e76d55afd0a826a944e40c795d92`; all four reviewed source files match after CRLF normalization only. Exact-commit CI run 116 (`35898538849`) started. Draft PR #9 was updated and remains unmerged.

## Continued diagnosis and sandbox access

A second original-budget scan failed with fixed classification `TRANSACTION_ACQUIRE_TIMEOUT` (P2028), not transaction execution expiry. An isolated read-only proxy with `maxWait: 10000` and the original `timeout: 5000` completed all 11 tenants/eight observations in 10.074 seconds. The code remediation will change only the health reader's acquisition budget and add fixed diagnostic phase/error codes; SQL, concurrency and tenant isolation stay intact. Hosted monitoring acceptance remains pending.

The user signed into staging. Read-only checks matched the displayed Kelly workspace uniquely to the existing sandbox connection and synthetic acceptance quote. Its local trial had expired, and it has neither Stripe customer nor Stripe subscription. Under the existing isolated sandbox testing authorization, a guarded tenant-scoped fixture update extended only that trial from `2026-09-22T21:31:56.259Z` to `2026-10-07T17:56:13.818Z`. The update required the exact staging environment/origin, sandbox mode, unique fixture match, unchanged prior expiry, trialing status, and absent Stripe bindings. No checkout, payment, production data or subscription was changed. The prior expiry is retained here for restoration.

## Monitor acquisition remediation verification

The bounded fix adds a ten-second acquisition allowance to the operational health row read while explicitly preserving its five-second execution limit. Tenant context binding, SQL and scan concurrency remain unchanged. Monitor failures now emit only allowlisted phases/codes and bounded elapsed time. Sentinel approved this scope with no blocking finding.

Full local `npm run verify:ci` completed with exit 0: 293 main unit tests, 407 database tests across 34 files, builds/lint/schema/security/route inventory, all evaluations, and root/web dependency audits passed. A real PostgreSQL regression holds the sole runtime connection for 2.5 seconds and verifies the tenant-scoped health read completes after waiting beyond the old two-second acquisition limit. Frozen six-source manifest: `.codex_tmp/qbo-monitor-remediation-manifest.json`. Opera independently approved that manifest and the evidence documentation for BCP and isolated staging monitor/API deployment. Hosted deployment/ten-cycle evidence remain pending; provider writes, alert-readiness acceptance, and production are not approved.

Operational limitations still to close before alert-readiness acceptance: persistent monitor cycle failures are recorded while the process stays alive, so Railway crash notifications alone do not detect that condition; additionally 16 serial eight-second delivery timeouts can extend a cycle. These are separate from the proven acquisition issue and are not claimed resolved by this fix.

## Hosted acquisition-fix checkpoint and follow-up

- The acquisition remediation was committed/pushed as `a8fb094ef2a81279f1057af88e267027b4522bdf`. Six reviewed source files match after CRLF normalization only. A clean LF archive verified all 698 tracked files, manifest digest `77aeefa21b10b6cf600b189f3d4e3cf276f0c1d08b6f96f5d826cfb05bc45c1f`.
- Monitor deployment `0cff4c3f-476d-45c5-a3a5-5b119d1d8d3e` started at 18:09:45 UTC. API deployment `efb3b706-380e-434c-9343-c8d9d954b8f5` reached SUCCESS. Five content-hash probes match a8fb094 on both deployed processes. API flags remain unchanged; web and reconciliation worker remain on 98c7575.
- Exact tax checkpoint CI run 116 completed SUCCESS. Exact a8fb094 CI run 117 (`35900236726`) is running at this checkpoint.
- The monitor observation window did **not** pass: samples 3, 7 and 10 recorded FAILED, with fixed diagnostics locating a separate P2028-class error in `alert_evaluation`. Successful cycles were approximately 9.6-13.2 seconds. The ten-cycle window ended at 18:20:09 UTC with `passed: false`. No failure was hidden or accepted as a healthy window.
- A rollback-only hosted evaluation measured 5.084 seconds for the original eight individual state reads; a diagnostic using one batched state read took 3.484 seconds. Neither diagnostic persisted state or sent email. A narrow source optimization now reads the fixed eight states once under the existing advisory transaction lock; all decisions, upserts and outbox writes remain in that transaction.
- OPS-1 follow-up now terminates after three consecutive failed complete cycles, preserves FAILED evidence, resets only after a complete success, and retains graceful signal handling. Delivery claims have a 20-second monotonic budget and the existing 16-count cap; already-started sends finish their bounded durable handling. The four-file OPS-1 candidate passed full verify with 301 unit tests and scoped security approval. The combined five-source candidate (including the batched state read) is running verify:ci and independent review.

The combined candidate subsequently passed `verify:ci` with exit 0: 301 main unit tests and all 407 database tests/34 files, plus builds/lint/schema/security/inventory/evaluations/audits. Preserved log: `.codex_tmp/qbo-monitor-reliability-verify-ci-passed-20260923.log`; five-source manifest `.codex_tmp/qbo-monitor-reliability-manifest.json` still matches. Sentinel approved the batched state read's serialization and atomicity. Final Opera verdict is APPROVED for BCP, isolated staging monitor deployment, and the reviewed bounded fault test, with all five manifest hashes matching and 18 independent focused tests passing. Stable hosted cycles, exactly three induced failures, nonzero exit, independent Railway notification receipt, restart and quiet recovery still must be demonstrated before operational-alert readiness is accepted. Production and provider writes remain unapproved.

The independent monitor-failure proof must induce three separate failures. A fixed long advisory lock is unsuitable because Prisma may surface its transaction timeout only after the lock releases (local eight-second hold produced P2028 after 8.02 seconds). The revised ignored helper observes one waiter on the exact advisory key through count-only `pg_locks`, holds 6.5 seconds beyond that observation, then releases; it repeats three bounded times. Its local variant uses a distinct key and proved three P2028 failures. No hosted fault has run yet. Hosted guards require the exact isolated staging API/environment, origin and sandbox, and every held transaction has a 90-second timeout with a 75-second waiter deadline.

## Additional invoice-numbering prerequisite

Current [Intuit Invoice documentation](https://developer.intuit.com/app/developer/qbo/docs/api/accounting/most-commonly-used/invoice) warns against custom document numbers when custom transaction numbering is disabled. The actual [Preferences field](https://static.developer.intuit.com/sdkdocs/qbv3doc/ippdotnetdevkitv3/html/d74949bd-5060-c05e-e5bd-54bcae224a49.htm) is `SalesFormsPrefs.CustomTxnNumbers`. The existing NON publisher supplies a stable custom number but does not verify this setting before CREATE. Existing ambiguous-result recovery remains conservative and does not blindly recreate, but a provider-changed number could make number-based recovery unresolved; this has not been observed in the sandbox.

A separate isolated implementation is adding an explicit preference check and strict zero-collision query immediately before CREATE, with fixed proven-no-write failure handling and reviewed-retry tests. No provider settings will be changed automatically. This guard and its real-provider evidence are required before the first sandbox invoice write. Tax proof also still needs provider calculation, reviewed origin/destination inputs, and exact Estimate-to-full-Invoice canonical parity; no progress-invoicing support is claimed.

## Hosted monitor reliability proof

- Combined monitor candidate committed and pushed as `c7252ff1d928a0d3642df6200b0213d6a970f9b8`. Clean LF archive verified all 700 tracked files; digest `976b383565a200377d7f566b17e412753f14a849ed2e32c0571fc8d0562e8563`. Deployment `22daba1d-0686-49ed-92b8-446f325f2594` reached SUCCESS, with all seven selected deployed source hashes matching. API remains a8fb094; worker/web remain 98c7575.
- Exact a8fb094 CI run 117 completed SUCCESS. Exact c7252ff CI run 118 (`35902462023`) remains in progress at this checkpoint.
- Monitor observation passed ten distinct future cycles from 18:29 through 18:38 UTC, with unchanged process start `18:28:21.238Z`, all RUNNING, durations 8.297–11.751 seconds, and no failed-cycle events. Evidence: `.codex_tmp/qbo-monitor-stable-window-c7252ff-20260923.jsonl`.
- Fresh containment confirmed exactly one monitor service instance, staging only, no repository/image source, dedicated start, no HTTP health check, ON_FAILURE with three restart attempts. Fresh API flags confirmed sandbox, OAuth-only, reconciliation/CDC/payments disabled, verifier/key present, and no migration-owner database URL.
- The first fault helper invocation exited before reporting an acquired lock; its raw error was omitted and no fault was claimed. Fixed allowlisted phase/error diagnostics were added to the ignored helper. The subsequent guarded invocation acquired and released exactly three bounded locks at 18:40–18:42 UTC, observing one waiting evaluator each time. All locks were released; helper exited 0 at `18:42:37.287Z`.
- Hosted logs show exactly three `alert_evaluation` failures and `quickbooks_monitor_failure_limit_reached`. Durable FAILED heartbeat was observed during the test. Railway automatically restarted the same deployment: new process start `18:42:37.864Z`, RUNNING heartbeat `18:42:51.602Z`. The nonzero exit is corroborated by the reviewed exit-code path and ON_FAILURE restart; no raw exit-code field was retrieved.
- Independent Railway email receipt was requested from the owner and remains unproven. Railway's [deployment actions documentation](https://docs.railway.com/deployments/deployment-actions) describes terminal Crashed notification after restart exhaustion, so one successful automatic restart alone does not establish email delivery.
- Controlled reconciliation-worker SIGTERM sent at `18:43:23.252Z`, after unique live process identity validation. OPEN/recovery delivery and the quiet window are pending; the worker will be explicitly restarted after OPEN evidence.

The isolated numbering guard's initial database suite passed 58/58 after correcting two too-short synthetic idempotency keys. Independent backend review nevertheless found release blockers: preflight reads can outlive the durable publish claim; provider identity is not bound before the confirmation read; refresh replay needs phase-aware ambiguity handling; reconnect generation must bind review/claim/fence. These are being remediated before merge or hosted invoice writes. Four fixed numbering errors now have tested English/Spanish mappings in the isolated candidate (13/13 localization tests). No collision query is claimed to guarantee global provider-number uniqueness.

## Worker alert delivery test: configuration blocker

The worker persisted STOPPED at `18:43:24.279Z`. The monitor opened WORKER_HEARTBEAT incident generation 1 at `18:43:40.236Z` and retained its failed email as a PENDING outbox delivery. A guarded staging-only diagnostic invoked the existing delivery/claim function with its immutable payload and idempotency key, emitting only HTTP status and fixed reason. Resend returned HTTP 401, classified `API_KEY_INVALID`; no provider body or key was printed. Delivery is **not** accepted as successful. The owner was asked to replace the staging API's RESEND_API_KEY through the provider editor; the monitor will reference that configuration and be restarted after confirmation.

The first Railway worker restart command failed with a transport error; a fresh heartbeat still showed STOPPED before retry. The second command succeeded for the existing deployment. Worker process start is now `18:46:15.271Z`, with fresh RUNNING heartbeats. No rebuild, provider-write enablement, or production action occurred. Recovery transition/delivery and the duplicate-free quiet window remain pending. The outbox is preserved for its bounded normal retry policy.

The monitor recorded the RECOVER transition for the same incident at `18:47:40.237Z`; the state is HEALTHY/inactive. Both OPEN and RECOVER deliveries remain PENDING because of the invalid email credential. Sanitized fault-log capture confirms exactly three `TRANSACTION_EXECUTION_TIMEOUT` failures, one failure-limit event, and the first recovered cycle. Exact c7252ff CI run 118 subsequently completed SUCCESS, including the remote launch-readiness gate. A fresh ten-cycle post-fault observation is running; this does not substitute for actual email acceptance and inbox receipt.

The post-fault observation completed successfully at `18:58:55.510Z`: ten RUNNING cycles, unchanged restarted process, durations 8.282–12.130 seconds. At `18:58:29.384Z`, both processes remained RUNNING and the worker incident remained HEALTHY/inactive. Its only two delivery records were the original OPEN and RECOVER; neither was SENT. This proves stable recovery and no duplicate outbox incidents over more than ten minutes, not successful email delivery or duplicate-free inbox behavior.

## Numbering and publish-claim remediation candidate

The isolated candidate now includes a shared 20-second numbering-read deadline across OAuth refresh, an awaited tenant/operation/claim/connection/realm/generation fence immediately before each CREATE, phase-aware failure handling, and immediate binding of the returned provider invoice ID before the confirmation GET. The original 120-second claim lifetime is unchanged. Late identity retention only accepts the same immutable request/payload/connection attempt; it preserves reclaimed reconciliation work and moves a matching failed attempt with new financial evidence into reconciliation. Reviews bind the reconnect generation. The provider invoice ID is bounded to the database column length.

Renford's independent final source verdict is APPROVED. Focused verification passed backend compile, 54 provider unit tests (including 14 new numbering cases), and all 75 invoice database cases. The first expanded 71-case run found three issues and was corrected before the 75-case rerun; only the final result is passing evidence. Root copied the frozen five backend/test files plus four localized-copy/test files, excluded the generated sitemap status-only change, and registered the new unit file in the regular gate. The exact ten-source manifest is `.codex_tmp/qbo-numbering-final-manifest.json`. Full merged `verify:ci` and Sentinel/Opera review are pending at this checkpoint. No new accounting write was enabled.

Full merged `npm run verify:ci` completed with exit 0 at 19:00 UTC: 315 main unit tests (including the new 14-case numbering suite), 427 database tests across 34 files, builds/lint/schema, security checks, route inventory, evaluations, and dependency audits. Preserved output: `.codex_tmp/qbo-numbering-verify-ci-passed-20260923.log`. All ten frozen source hashes still match. Final independent security/Opera verdicts and any resulting commit remain pending; provider accounting writes remain disabled in the staging API.

## Reviewed source saved and staging rollout

Sentinel and final Opera independently returned APPROVED with no findings for BCP and the exact OAuth-only isolated staging API/web candidate. Opera also independently ran all 27 numbering/localization cases successfully. Production/provider-write enablement remains BLOCKED_MISSING_EVIDENCE; no release claim was expanded.

Source committed and pushed as `6e25a40493998f2b2e82e84282a2e0864d0c73a6` on the existing staging branch. All ten reviewed files match committed content after CRLF normalization only. Its clean LF archive verified all 701 tracked files, contains no tracked environment secrets, and has digest `7b4dd36c11898ed73f132e19b106a2afe5dcad49f370fe71e2d4f7d080df0681`. Draft PR #9 remains unmerged. Exact source CI run 119 (`35907314540`) started and is not yet claimed passing.

Matching isolated API deployment is `50c363da-1f8d-4969-99f2-5405342b80dc`; it is building at this checkpoint. Matching staging-project web deployment `dpl_5RwYEXTWcyV1LAadLb1VkZLqg4E9` is READY, created with automatic domain promotion disabled. Promotion and deployed source/configuration checks are pending. The accounting worker remains 98c7575 and the operational monitor remains c7252ff. No migration is required for this candidate.

Owner actions still pending: replace the invalid staging Resend key through the provider editor; confirm independent Railway failure-email receipt; sign into the existing Intuit developer account. Do not retrieve or paste credentials. After the mail configuration is applied, prove accepted OPEN/RECOVER delivery, actual inbox receipt and quiet recovery. Then gather sanitized Intuit preferences/query fixtures, signed-webhook delivery, guarded sandbox invoice CREATE/readback/reconciliation and tax/canonical-parity evidence before advertising or production enablement.

The API deployment subsequently reached SUCCESS; the prior API deployment was removed. All five deployed source probes (package, routes, provider, invoice service and tax capability helper) match 6e25a40. Health and database readiness both returned HTTP 200. Fresh presence-only checks confirm sandbox/OAuth-only mode, API reconciliation/CDC/payments disabled, verifier/encryption key configured, and migration-owner URL absent. The matching web build was promoted successfully within `quotefly-staging` only. Browser reload restored the existing session and visibly showed Quotefly Sandbox, Connection verified, and the explicit connection-only staging limitation. No production promotion or accounting write occurred.

## Independent platform terminal-failure test

Opera approved a separate bounded staging test of the existing ON_FAILURE/three-retry policy, without changing code or policy. The maximum was twelve induced failures (four process lifetimes, three failed cycles each), stopping at an earlier terminal state. Fresh preflight confirmed the exact single staging monitor process/deployment, seven matching c7252ff source hashes, healthy heartbeat, unchanged restart policy and sandbox/OAuth-only API safeguards. The accounting worker was not stopped in this test.

The helper began at `19:22:20.563Z`. Railway reached CRASHED at `19:29:28.943Z`, after nine induced `alert_evaluation` failures and three failure-limit events in this run; the earlier fault test had already used a restart. No unexpected failure phase or deployment/policy drift was observed. This terminal event is the relevant native-notification test and supersedes the earlier 11:42 a.m. Pacific receipt request. The owner was asked to confirm the Railway email around **12:29 p.m. Pacific**; actual inbox receipt remains unproven.

After terminal failure, the helper's next waiting transaction reached its bounded no-evaluator deadline and exited with the fixed `NO_EVALUATOR_WAIT` diagnostic. This expected helper exit 1 is not mislabeled as a successful twelve-attempt run. At `19:30:54.644Z`, an independent count-only proof showed zero holders and zero waiters on the exact advisory key. Only then was Railway Restart invoked for the same deployment. Railway returned it to SUCCESS at `19:31:01.693Z`; seven source hashes and ON_FAILURE/three retries remain unchanged. Durable FAILED evidence was captured before restart, while the accounting worker remained RUNNING. Recovery-cycle checks are in progress; no email delivery or production readiness claim is made.

Final Opera also approved documentation-only BCP for the operational ledger and subsequent strictly factual deployment/CI/health additions, provided source and readiness scope remain unchanged. No source gate is bypassed or weakened by that allowance.

Final recovery passed three distinct future cycles at 19:32–19:34 UTC, durations 9.711, 8.421 and 8.094 seconds, with unchanged new process start `19:31:01.816Z`. At `19:34:47Z`, both worker heartbeats were fresh RUNNING, the single worker incident remained HEALTHY/inactive, and only its original OPEN/RECOVER deliveries existed; both remain PENDING because the Resend credential has not been replaced. Zero advisory-lock holders/waiters were independently verified after recovery, and API health/readiness both returned 200. No test fault helper remains running.

Exact deployed source `6e25a40` subsequently completed GitHub CI run 119 (`35907314540`) with SUCCESS, including the remote launch-readiness gate. The remaining working-tree change is this factual operational ledger; no source, schema, provider flags, recipient, restart policy, or production configuration changed after approval. The next required owner steps remain the staging Resend key, Intuit sign-in, and receipt confirmation for the 12:29 p.m. Railway terminal-failure email. These unresolved external checks prevent a production or provider-write readiness claim despite the green code gates.
